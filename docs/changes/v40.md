# v40 — finish the reported cutoff presentation repairs

## Returned result

v39 passed 17 of 19 gates. The window and truncation modules are newly accepted,
with 21 exact-type and transitive-axiom reports. Across all passing gates, 168
distinct declarations have only the allowed axioms. Passing logs are warning-free.
The cutoff module has three reported errors; the prime-mass gate was blocked.

## Repair scope

Only `research/Erdos647Research/Endpoint/CutoffParameters.lean` changes among
mathematical modules. No existing declaration header, hypothesis, constant,
parameter definition, or audit target changes.

1. Use an explicit conversion plus rational normalization for the limit
   `(4 * (1/50))^-1 = 25/2` instead of relying on default `simp` normalization.
2. Use the existing `Pi.div_def` and `zero_div` identities to turn a quotient of
   functions into its explicit pointwise lambda with limit zero.
3. Apply `div_div_div_cancel_right₀` to both logarithmic numerators explicitly,
   retaining the proof that the common scale is nonzero.

The existing 19-gate sequence, warning policy, exact-type examples, axiom
allowlist, dependency pins, cache paths, CI settings and entry points are unchanged.
The runner changes only its unique diagnostic prefix to `erdos647_repo_v40_results_`.
No package bootstrap or architecture refactor is introduced.

## Mathematical target

Next establish the prime-cutoff growth/ordering and the exact prime-mass
expansion. Only then derive a fixed positive gap against the signed corrected
budget, bound every amplified error, and assemble the endpoint at a comfortable
explicit fixed c>0. Neither c=1/1000 nor another numerical output target is required.
The exponent a and scale (log X)^a / log(log X) stay fixed. No output coefficient
is claimed in this repair.

The new mathematical PR remains pending the last two parameter gates. The prior
14-gate fixes PR remains supported by its accepted v36 evidence.
