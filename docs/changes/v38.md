# v38 — first endpoint-parameter branch overlay

New mathematical effort: prove the exact protected parameter asymptotics and
specialize the accepted selected-prime Mertens limit. Scope and formulas are in
[the branch charter](../endpoint-parameter-branch.md).

Adds five research modules and five exact-type/axiom audits (42 theorem declarations),
one experimental research Lake library, five gate registrations, and regression
coverage for the new boundary and exact integer rounding.

All 40 accepted mathematical modules and all previous audits remain byte-identical.
No protected definition, dependency pin, allowed axiom, warning policy, core build
configuration, CI workflow, or Git ignore rule changes. The existing full mode
still runs all registered gates; Lake controls incremental rebuilding. The only
runner implementation change is the diagnostic version prefix.

The v34 registry-size regression is extended to allow the new endpoint statement gate and four parameter gates
while retaining its check for all 14 baseline gates. New tests check the exact
19-gate union, independent parameter prerequisites, and the specialized analytic
boundary. No proof-verification check is removed.

The previous fixes PR remains ready on its accepted snapshot. The new parameter
PR has its own pending build/type/axiom acceptance; it is not an extension of that
fixes PR. The budget-gap and final endpoint are not declared proved here.

## Revised coefficient requirement

Adds EndpointBound c and PositiveEndpointClaim in research only. They preserve the
critical exponent and asymptotic scale. The historical hardcoded 1/1000 target is
unchanged but optional. The coefficient is fixed independently of X, and no
endpoint bound for any positive coefficient is claimed yet. The working candidate
1/2000 is explicitly labeled provisional in the coefficient-status record.
