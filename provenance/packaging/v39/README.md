# v39 parameter-rewrite repair

This is an overlay with no enclosing folder. It changes two pending mathematical
modules, adds a shared real-power/log adapter and its exact-type/axiom target,
and records the revised endpoint strategy. Existing accepted proofs, parameter
definitions, toolchain pins, warning policy and CI remain unchanged.

The prior-run evidence records 15/19 gates passed, including the newly accepted
coefficient-parametrized statement interface. No endpoint bound or positive
coefficient is asserted. See docs/changes/v39.md and the exact source diff.

The validation JSON distinguishes package tests and preservation checks from
the next Lean gate results. No successful parameter-gate report is manufactured.
