# v36 — final lint repair and focused fixes PR

## Returned evidence

The submitted run `erdos647_repo_v35_results_20260908T071828Z_a8ef3d01`
passed 13 of 14 gates. The 139 distinct declarations in its passing axiom reports
use only `propext`, `Classical.choice`, and `Quot.sound`.

The restricted PNT+ boundary passed both its recursive 14-module source check
and the `MediumPNT`/adapter theorem audits. The fixed-constant small-prime debit
estimate also passed, including its exact expanded statement.

The sole failed build was `corrected_budget_estimate`. At line 52, Lean reported
`linter.unnecessarySeqFocus`: `field_simp [...] <;> ring` used all-goals sequencing
where ordinary sequencing suffices. Its exact-type/axiom audit did not run.

## Minimal repair

`CorrectedBudgetEstimate.lean` now sequences `field_simp` and `ring` on separate
lines.

No theorem statement, hypothesis, constant, import, numerical budget convention,
exact-type target, dependency revision, or axiom rule changes. No warning is
suppressed. Both package builds retain `-DwarningAsError=true`.

The runner changes only its diagnostic filename prefix to
`erdos647_repo_v36_results_`. The archive-name test now checks the versioned name
format and its equality to the current run ID, rather than hardcoding v35.
The default still selects all 14 gates, using Lake incremental builds and fresh
exact-type/axiom reports for each selected gate.

## PR scope and readiness

The included PR description summarizes the accumulated repository fixes:
independent finite core, separate research module ownership, the restricted PNT+
compatibility boundary, strict source/axiom checks, and warning cleanup. It does
not claim to be a comparison against an inspected remote Git base.

The changeset is suitable for a draft/review PR. Merge acceptance still requires
a passing current all-gates run including `corrected_budget_estimate`. The v35
run remains explicitly 13/14, not relabeled as a successful v36 run.

The endpoint theorem is outside this PR's acceptance scope. No remote branch or
PR is created by the overlay, and no push or CI-trigger change is included.
Tools, dependency caches, previous results, and existing CI configuration remain
untouched. This overlay is for the current v35 repository; it is not a cumulative
installer for historical versions.
