# Accepted final endpoint — v47

Run: `erdos647_repo_v47_results_20260908T205250Z_0078dcb7`.

All 29 gates passed; 235 distinct declarations have permitted transitive axioms.
The run includes 194 exact-type/definition examples in 30 audit files and 284
passing runner tests. Build and audit logs contain no warning diagnostics.

The final theorem proves `EndpointBound c` for each fixed `c < 1499/1000000`.
For a genuine saving take `0 < c < 1499/1000000`; the explicit `c=1/1000`
corollary, `PositiveEndpointClaim`, and historical `EndpointClaim` all passed.
The upper endpoint of the coefficient range is NOT included. The onset is
existential and may depend on c; this is not a finiteness or existence resolution.

`summary.json`, `logs/`, `source/`, and `MANIFEST.json` preserve the supplied
record. `REVIEW.json` is the source/log consistency review. These are historical
v47 results, not a successful execution of the v48 relocation.

The fixed restricted PNT+ source closure has 14 modules; excluded upstream
catalogues are not thereby certified. No independent kernel implementation or
external novelty review is claimed.
