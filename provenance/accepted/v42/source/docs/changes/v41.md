# v41 — accepted parameter-asymptotics checkpoint

The v40 results passed 19/19 gates, with 183 distinct audited declarations, no
build/audit warnings, 127 exact-type examples and 178 passing tooling tests.
Fifteen cutoff/prime-mass declarations are newly accepted.

This is a documentation/evidence-only overlay. It updates the parameter PR body,
proof-status and coefficient records, README status sections and research README;
adds the next fixed-positive-gap assignment; and preserves the accepted v40 logs
and original diagnostic ZIP. The previous document versions remain in provenance.

All 45 mathematical modules, 20 audit files, tests, checking code, Lake configuration,
lockfiles, tools, caches, previous results and CI settings are untouched. The runner
keeps its v40 archive label intentionally. No repeat run is needed for a proof fix,
because the supplied results contain no remaining failure.

The local acceptance condition for a normal parameter-asymptotics PR is satisfied.
The next mathematical changeset is the positive mass-budget gap and supporting
size estimates, followed by all amplified errors and final endpoint assembly.
No fixed positive endpoint coefficient or resolution of #647 is claimed.
